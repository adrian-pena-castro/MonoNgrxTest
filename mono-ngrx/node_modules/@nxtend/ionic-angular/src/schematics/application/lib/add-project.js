"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.addProject = void 0;
const workspace_1 = require("@nrwl/workspace");
function addProject(options) {
    return workspace_1.updateWorkspaceInTree((json) => {
        let assets = json.projects[options.projectName].architect.build.options.assets;
        assets = assets.filter(
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        (asset) => !asset.toString().includes('src/favicon.ico'));
        assets.push({
            glob: '**/*.svg',
            input: 'node_modules/ionicons/dist/ionicons/svg',
            output: './svg',
        });
        json.projects[options.projectName].architect.build.options.assets = assets;
        const styles = json.projects[options.projectName].architect.build.options.styles;
        styles.push({
            input: `${options.appProjectRoot}/src/theme/variables.scss`,
        });
        return json;
    });
}
exports.addProject = addProject;
//# sourceMappingURL=add-project.js.map
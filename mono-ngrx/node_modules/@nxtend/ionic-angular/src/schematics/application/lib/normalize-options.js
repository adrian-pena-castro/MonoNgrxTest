"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.normalizeOptions = void 0;
const workspace_1 = require("@nrwl/workspace");
const ast_utils_1 = require("@nrwl/workspace/src/utils/ast-utils");
const path_1 = require("path");
function normalizeOptions(host, options) {
    const appName = options.name;
    const name = workspace_1.toFileName(options.name);
    const prefix = workspace_1.getNpmScope(host);
    const appDirectory = options.directory
        ? `${workspace_1.toFileName(options.directory)}/${workspace_1.toFileName(options.name)}`
        : workspace_1.toFileName(options.name);
    const projectName = appDirectory.replace(new RegExp('/', 'g'), '-');
    const appProjectRoot = path_1.normalize(`${ast_utils_1.appsDir(host)}/${appDirectory}`);
    return Object.assign(Object.assign({}, options), { appName,
        name,
        prefix,
        projectName,
        appProjectRoot });
}
exports.normalizeOptions = normalizeOptions;
//# sourceMappingURL=normalize-options.js.map
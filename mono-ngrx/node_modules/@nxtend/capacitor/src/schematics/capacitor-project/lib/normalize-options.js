"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.normalizeOptions = void 0;
const workspace_1 = require("@nrwl/workspace");
const path_1 = require("path");
function normalizeOptions(options, host) {
    const appName = options.appName ? options.appName : options.project;
    const { root: projectRoot } = workspace_1.getProjectConfig(host, options.project);
    const pathToRoot = path_1.relative(`${process.cwd()}/${projectRoot}`, process.cwd()).replace(/\\/g, '/');
    const webDir = options.webDir ? options.webDir : `dist/${projectRoot}`;
    return Object.assign(Object.assign({}, options), { appName,
        webDir,
        projectRoot,
        pathToRoot });
}
exports.normalizeOptions = normalizeOptions;
//# sourceMappingURL=normalize-options.js.map